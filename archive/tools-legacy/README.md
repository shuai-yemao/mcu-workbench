# 旧工具 Skills 归档

这里保留工具方向重分类前的 29 个入口目录，供历史文档、迁移和兼容解析使用。

- 归档目录不属于插件 manifest，不会作为独立 active skill 加载。
- 旧名称由 `skills/catalog.js` 解析到 `skills/tools/` 下的 7 个 canonical skill。
- 原入口中的 `references/`、`scripts/`、`templates/` 和其他资源已复制到对应 canonical skill 的命名空间下。
- 确认迁移完成前不要删除归档目录。
