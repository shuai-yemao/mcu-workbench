interface:
  display_name: "嵌入式 Adapter 适配"
  short_description: "将 BSP 抽象接口绑定到 MCU、RTOS 与硬件外设实现"
  default_prompt: "使用 $bsp-platform-adapter 将 BSP 抽象接口绑定到 STM32 外设和 RTOS。"

policy:
  allow_implicit_invocation: true
