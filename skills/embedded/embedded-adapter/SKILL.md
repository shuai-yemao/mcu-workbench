---
name: embedded-adapter
description: 设计和实现嵌入式 Adapter，将 BSP 的抽象函数指针绑定到 STM32 外设、GPIO、I2C、时间基准、RTOS 和中断服务，并支持 Mock 替换与构建验证。用户需要平台适配、硬件绑定或 Adapter 层测试时使用。
---

# 嵌入式 Adapter

## 工作边界

Adapter 是 BSP 与 MCU/RTOS 的唯一绑定层：上层只依赖抽象接口，Adapter 才调用 HAL、寄存器、GPIO、CMSIS-RTOS 或软件总线实现。

先读取 BSP 的接口结构体和状态码，再读取目标平台初始化、I2C/GPIO、时基、RTOS 和中断代码。AHT21 的实际映射见 [references/aht21-adapter-pattern.md](references/aht21-adapter-pattern.md)。

## 执行流程

1. **建立映射表**
   - 对每一个函数指针记录：上层语义、平台 API、参数转换、返回值转换和上下文限制。
   - 区分硬件 I2C、软件 I2C、延时、tick、锁、临界区、队列和线程接口。
   - 对每一个抽象接口标记是否允许阻塞、是否可在 ISR 调用以及是否需要互斥保护。

2. **实现最小包装**
   - Adapter 函数只做参数转换、平台调用、状态转换和必要的资源管理。
   - 平台初始化应幂等或明确拒绝重复初始化；失败必须返回可诊断状态。
   - 对指针、长度、地址、超时和句柄进行边界检查，避免让 HAL 接收非法参数。
   - 用静态接口表组装 Driver/Handler 所需依赖，避免把平台全局对象散落到业务代码。

3. **处理时序与并发**
   - 软件 I2C 的 SDA/SCL 翻转、ACK 等待和微秒延时必须保持确定时序。
   - 禁止在时序敏感路径打印日志；错误在路径结束后通过状态返回或低频诊断记录。
   - 明确总线锁、临界区和 RTOS 让出的范围，避免锁住延时或回调未知代码。
   - 对 tick 溢出、阻塞超时和 ISR/线程上下文分别处理。

4. **支持生产与测试替换**
   - 生产表绑定真实 HAL/GPIO/RTOS；测试表绑定 Fake/Mock，不修改 BSP 源码。
   - Mock 应可注入 ACK、读写错误、超时、返回数据和调用记录。
   - 不在生产 Adapter 中包含 Unity 测试入口或测试专用全局状态。

5. **验证绑定**
   - 静态检查每个函数指针是否初始化、类型是否匹配、状态码是否完整覆盖。
   - 先用 Mock 验证映射和错误转换，再用 Keil 构建验证 include、源文件分组和宏。
   - 目标板验证真实 I2C/RTOS/时基行为，记录 RTT 日志、错误码和关键时序结果。

## 设计禁区

- 不让 Driver 直接 include HAL 或调用 `HAL_*`/GPIO 宏。
- 不用日志替代错误返回，也不在 bit-bang I2C 循环中输出高频日志。
- 不把不同层的状态码强制当作同一个枚举；必须显式转换并保留错误来源。
- 不隐藏初始化顺序、锁策略、线程上下文和资源所有权。
- 不因为目标板暂时可用而省略 Mock、失败路径和正常固件构建。

## 交付检查

- 抽象接口到平台 API 的映射表完整。
- 所有函数指针已初始化且参数/返回值转换明确。
- 生产 Adapter 和 Mock Adapter 可互换。
- 时序、并发、ISR 限制和日志边界有文档记录。
- Keil 构建、测试构建和目标板运行证据与实际配置一致。
