interface:
  display_name: "BSP 外设 Handler 设计"
  short_description: "设计多实例外设 Handler 的资源、事件与错误处理流程"
  default_prompt: "使用 $bsp-peripheral-handler 设计一个位于 Driver 之上的多实例外设 Handler。"

policy:
  allow_implicit_invocation: true
