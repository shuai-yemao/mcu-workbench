# 学习辅导模式

先根据用户目标选择一个模式；没有明确目标时默认进入 `tutor`，不要同时执行三种模式。

| 模式 | 触发语句 | 主要输出 | 是否逐题等待 |
| --- | --- | --- | --- |
| `tutor` | 学习、掌握、讲清楚、通过提问理解 | 逐节教学、理解判断、分级 Q&A | 是 |
| `architecture-tutor` | 设计架构、划分模块、分析分层、解释取舍、迁移到另一平台 | 架构模型、逻辑推理、方案比较、验证计划 | 是 |
| `code-audit` | 分析文件、检查架构、找遗漏 | 证据表、调用链、覆盖缺口 | 否，先给审计结果 |
| `note-refresh` | 更新、补全、同步已有笔记 | 代码/笔记差异、修订草稿 | 默认否，写入前确认 |
| `project-note` | 整理项目、产出完整笔记 | 完整 Obsidian Markdown | 关键章节逐题确认 |

## 学习深度与模式的关系

每个模式开始时先判断学习深度（见 [learning-depth.md](learning-depth.md)）：

- **L1 会用**：`tutor` 模式按"最小必要学习清单"推进，不启动 `architecture-tutor`；`code-audit` 和 `project-note` 可正常使用。
- **L2 理解**：`tutor` 模式恢复完整提问流程；可切换到 `architecture-tutor`。
- **L3 系统掌握**：所有模式可用，`architecture-tutor` 为主要模式。

## 模式切换

- 用户回答"不知道"或要求"直接给答案跳过"：不自动跳过。以"基础概念 → 工程观察 → 最小推理 → 原题复答"闭环推进；先用当前工程现场的文件、符号、配置或现象建立可观察对象，再要求用户回答原题。
- 用户回答"跳过/这个会"：只有"这个会"且能用工程证据说明时才记录为已知；其余跳过请求仍按闭环补齐。用户明确说"结束本次学习"时，才可停止并记录未完成项。
- 用户要求直接出笔记：先完成最小扫描，再进入 `project-note`，不能跳过证据阶段。
- 用户要求直接设计方案：先进入 `architecture-tutor`，先让用户说明职责、约束和取舍，再形成方案草稿。
- 用户只要求分析代码：使用 `code-audit`，不要自动写入 Obsidian。
- 用户中断会话：输出当前章节、已确认答案、薄弱点、下一题和 Learning Backlog，供下次继续。

## 知识缺口处理

发现新知识时，先判断是否阻塞当前任务（见 [learning-backlog.md](learning-backlog.md)）：

- **阻塞** → 立即进入 L1 学习，不推迟；
- **非阻塞** → 记入 Learning Backlog（标为高频或后续），继续当前任务，不切换到学习状态。

## 停止条件

满足以下全部条件时，tutor 停止当前知识的学习，让用户回到项目：

- 当前问题已经解释清楚；
- 有明确实现方案；
- 可以写最小实验；
- 实验结果可以验证；
- 知道失败时从哪里排查。

用户出现知识扩张倾向（从一个知识点不断延伸到更底层的基础领域）时，主动提醒停止条件。不要求"把这个领域全部搞懂"。
