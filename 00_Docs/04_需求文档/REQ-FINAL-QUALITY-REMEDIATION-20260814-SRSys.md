# SRSys：最终质量整改闭环

## 系统需求

- 格式规则优先级：用户要求 → 项目配置 → 相邻源码 → 插件 profile；无前三层覆盖时采用 80 列基线。
- 必要注释覆盖模块/文件说明、公开 API、参数/返回值以及所有权、并发、硬件约束、错误恢复和非显然步骤。
- 整改不得改变函数签名、控制流、常量/宏、数据结构、资源/错误路径、包含依赖或分层关系。
- 复检失败、不可复现或工具不可用时输出 `阻塞`。

## 验收边界

本请求的验证仅覆盖插件源代码、主机测试和本机 Skill 镜像文件；不涉及交叉编译、烧录、目标运行或实物验证。
